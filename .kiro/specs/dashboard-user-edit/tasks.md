# Implementation Plan — dashboard-user-edit

> 発生源: GitHub Issue #259（登録済み利用者のロール・所属代理店・表示名の編集）。
> 依存する設計: `design.md`（無効化と共有する advisory lock・部分更新と所属の移動の区別・監査 action の正典化・行直下パネル・本番反映の順序）。
> 共有 DB テストの UUID 接頭辞は、DAL が **f9**（未使用を 2026-09-13 に確認）、ルート統合が f5 の空き番号。
> **本番反映の順序**: migration 0009 を新しいコードより**先に**本番へ当てる（逆順だと更新は確定するのに監査の INSERT が落ち、再試行でも監査は戻らない）。手順は 4.3。

- [ ] 1. 監査記録と DAL の基盤
- [x] 1.1 監査 action の正典化と、action の CHECK の拡張
  - 監査 action の一覧を 1 か所の正典（読み取り専用の配列）にし、型をそこから導く。既存の呼び出し側は変えずに通る。
  - 利用者の昇格・降格・所属の変更・表示名の変更を表す 4 つの action を正典へ加える。
  - migration で監査記録の action の制約を、明示の名前で既存 12 値＋4 値として作り直す。旧制約は名前で外し、名前が違えば失敗させる（黙って残さない）。ヘッダに「新しいコードより先に本番へ当てる」ことと、適用を確かめるクエリを書く。
  - SQL の網羅スイートに、明示名の制約がちょうど 1 つあり旧名が無いこと、新しい 4 値を受け付けること、未知の値を拒否することを加える。
  - DB テストで、正典の集合と DB の制約の集合が一致することを固定する。
  - 完了状態: 隔離 DB で SQL の網羅スイートと DB テストが緑。migration から値を 1 つ消すと集合一致テストが赤になることを確かめて戻し、その対照を記録している。
  - _Requirements: 5.2_
  - _Boundary: AUDIT_LOG_ACTIONS と 0009_

- [x] 1.2 (P) 保護付きの属性更新アクセサ（部分更新・所属の移動・最後の運営の保護）
  - トランザクションの最初に、無効化と**同じ** テナント単位の advisory lock を取る。ロック定数は「有効な運営の数を減らす操作を直列化する」意味の名前へ改め、値は変えない。既存の並行テストが持つリテラルはそのまま残し、コメントだけ新しい名前へ直す。
  - 対象を運営で絞って取得し、無ければ「対象なし」。所属の移動なのに対象が代理店ロールでなければ「ロールが変わった」。所属先を運営で絞って確かめ、無ければ「代理店なし」。この順序を守る（利用者・代理店の存在を漏らさない）。
  - 対象が有効な運営で代理店ロールへ変わる場合だけ有効な運営の数を数え（保留中を含む）、1 以下なら「最後の運営」。無効化済みの運営の降格は判定しない。
  - 指定された項目だけを更新し、差分が無ければ更新せず現状を返す。有効／無効・メール・認証主体には触れない。拒否時はすべて巻き戻す（同じ入力の表示名も確定しない）。
  - 結果は、更新（前後の DB の行つき）・最後の運営・ロールが変わった・代理店なし・対象なし、を判別できる形で返す。
  - 1.1 と並行させるなら別の worktree で行う。隔離 DB は migration を全件適用し、`@fwlm/db` のテストには 1.1 の集合一致テストも入るので、同じツリーでは 1.1 の途中状態に巻き込まれる。
  - 完了状態: 実 DB テスト（f9・逐次）で、昇格・降格（保留中の運営も数える）・代理店の変更・所属の移動・表示名を未設定にする・変化なし・最後の運営の拒否（表示名も不変）・無効化済みの運営の降格・運営ロールへの所属の移動の拒否（他運営の代理店を指定しても同じ結果）・他運営／不在の代理店・他運営の利用者（他運営の代理店を指定しても対象なし）・保留中の利用者の初回ログインで新しいロールが返る・有効／無効とメールが不変、がすべて緑。
  - _Requirements: 1.2, 1.3, 1.4, 1.5, 1.7, 1.8, 1.9, 1.11, 1.13, 2.3, 2.4, 2.6, 3.1, 3.2, 3.3, 3.4, 4.1, 4.3, 4.4, 4.5_
  - _Boundary: updateDashboardUserGuarded_

- [x] 1.3 降格と無効化の並行安全性の決定的検証
  - 別の接続でテナントのロックを保持したまま操作を未確定にし、本物の関数が 500ms 解決しないこと、確定後に最新状態を観測することを確かめる。組み合わせは 3 つ: 無効化を保持中の降格、降格を保持中の無効化、降格を保持中の降格。
  - lifecycle の design の「並行ガードの正当性」へ、直列化の対象を降格まで広げたことの相互参照を 1 行足す。
  - 完了状態: 3 つの並行テストが決定的に緑で、どの順序でも有効な運営が最低 1 名残る。更新の関数からロックを外すと「無効化を保持中の降格」が必ず赤になることを確かめて戻し、その対照を記録している。
  - _Requirements: 2.5_

- [ ] 2. dashboard-api: 更新エンドポイント
- [x] 2.1 更新の入力検証・自己判定・結果の写像・監査の導出
  - 着手前に `@fwlm/db` を再ビルドする（dashboard-api は `dist` を経由して読むため、1.1・1.2 の型がビルドされていないと古い型のまま進む）。
  - 運営限定の認可の後、利用者 ID の形式を検証し（不正は「見つからない」）、小文字へ正規化してから扱う。
  - ロールと所属の組み合わせの検証を登録と共有する形へ切り出す（登録の挙動は変えず、既存の登録テストは緑のまま）。
  - body を「ロールを変える」「代理店ロールのまま所属を移す」「表示名」へ写す。何も変更が無い body・型の誤り・ロール無しの null の所属は入力エラー。
  - 対象が自分自身で、ロールを代理店にする、または所属を移す場合は、DB に行かずに専用の 409 で拒否する。自分の表示名だけの変更は許す。
  - 更新の結果を 200・409（最後の運営／ロールが変わった）・404（対象なし／代理店なし）へ写す。message は内部の詳細を含まない日本語の固定文。
  - 前後の行の差分から監査 action を並べる純関数を作り、更新が成功したときだけ action ごとに監査を書く（降格は 1 件・変化なしは 0 件・表示名の値は残さない）。
  - 完了状態: ハンドラの単体テストと dashboard-api の型検査が緑（型だけの依存は vitest では検出されないので、型検査を必ず通す）。単体テストは横断ガードの表に加わり、入力エラーの各形・大文字の自分の ID でも 409・自分の表示名だけなら 200・結果の写像・依存へ渡す引数（小文字化した ID と認証由来の運営）・監査の action と件数と順序（拒否と変化なしは 0 件）・差分→action の全規則を確かめている。正規化を外すと大文字 ID のテストが赤になることを確かめて戻し、その対照を記録している。
  - _Requirements: 1.5, 1.6, 2.1, 2.2, 2.6, 3.4, 4.1, 4.2, 4.3, 4.6, 4.7, 5.1, 5.3, 5.4, 5.5_
  - _Depends: 1.1, 1.2_

- [x] 2.2 更新ルートと合成根の配線（依存一式を組み立てる全箇所の同時更新）
  - 更新ルートを足す。JSON として壊れた body は入力エラー。合成根で更新の実依存と監査の実依存を配線する。
  - 依存一式を組み立てる全箇所（アプリ結線テスト・招待／リンク統合・店舗登録統合・ルート統合）に更新の依存を足し、パッケージ全体の型を緑に保つ。ルート統合の組み立てには監査の実依存も配線する。
  - ルート統合テスト（実 DB）: 未認証 401 と代理店 403 の表に加える。運営の更新で DB が変わり、監査記録に新しい action の行が 1 行増える。自分のロール変更は 409 で行が変わらない。他運営の利用者は 404。他運営の代理店は「代理店なし」。運営ロールへの所属の移動は 409。
  - 更新後の権限が次の要求から効くことを、実物の認証解決を通して観測する。リンク済みの代理店利用者の所属を移した直後、その利用者の店舗一覧が新しい代理店の範囲で返り、元の代理店の店舗は移っていない。降格した直後の利用者は管理 API で 403 になる。
  - どちらの観測も、新しい f5 のリンク済み利用者をテストの中で作って行う。降格側は有効な運営を使い、降格の前に同じ要求が 200 であることを対照として確かめる（既存フィクスチャのリンク済み運営は無効化済みなので、降格の前から 403 になり空振りする）。既存の共有フィクスチャと「行為者が唯一の有効な運営」という前提は動かさない。
  - 完了状態: dashboard-api のビルド・型検査・lint・テスト（隔離 DB の DB テストを含む）が緑で、予約パスのガードも緑。
  - _Requirements: 1.10, 1.11, 3.4, 4.1, 4.2, 4.3, 4.4, 5.1_
  - _Depends: 1.1, 1.2_

- [ ] 3. dashboard-web: 編集 UI
- [x] 3.1 (P) API クライアントに更新を追加
  - 更新の呼び出しを足す。利用者 ID を URL 符号化して POST する。body は「運営にする」「代理店にする（代理店を添える）」「代理店ロールのまま所属を移す（ロールを入れない）」の 3 形と、表示名（変えたときだけ）へ写す。
  - 完了状態: クライアントのテストで、URL・メソッド・3 形の body と表示名の有無の完全一致が緑。
  - _Requirements: 3.1, 3.4_
  - _Boundary: updateDashboardUser_

- [x] 3.2 行直下の編集パネル部品
  - 見出しつきのカードに、ロール・所属代理店（代理店ロールのときだけ）・表示名の入力と、保存・取りやめを置く。入力の ID は行ごとに一意にし、登録フォームと衝突させない。広い版面での幅の段は既存の 4 面と揃える。
  - 自分の行、または代理店一覧を取得できていないときは、ロールと所属を文字で固定表示し理由を添え、表示名だけを受け付ける（現在の所属を未選択に見せない）。
  - ロールを現在と異なる値に選んだときだけ、変更後に利用できる範囲の案内を出す。
  - 開いた時点の値と比べて変更した項目だけを送る（ロールが違えばロールの変更、代理店ロールのまま代理店だけが違えば所属の移動）。変更が無ければ送らずに閉じる。代理店ロールで所属が空なら送らずに案内する。
  - 送信中は保存を無効にしつつ焦点を失わせない。失敗したら入力を保持し、コードから引いた固定文言の警告を 1 件だけ出す（サーバの message は描かない・未知のコードは汎用文言）。
  - 完了状態: 部品の単体テストで、初期値・所属欄の出し分け・ロール変更の案内・自分の行と一覧取得失敗の固定表示・所属未選択の検証・表示名の trim と空の扱い・送る項目の完全一致（ロールの変更と所属の移動）・変更なしで送らない・コード→文言（未知のコードを含む）・二重送信の防止・失敗時の入力保持と警告 1 件が緑。
  - _Requirements: 1.1, 1.5, 1.6, 1.8, 1.13, 1.14, 2.2, 2.7, 3.1, 3.2, 3.4, 4.7, 6.6, 6.7, 6.8, 6.10_

- [ ] 3.3 利用者一覧に表示名の列・編集ボタン・パネル行を統合
  - 一覧に表示名の列を足し（未設定は未設定と分かる表示）、列数を 1 か所で持つ。すべての行に編集ボタン（開閉の状態・制御先・見えている文言を含む読み上げ名）を置く。
  - 編集を始めると対象行の直後にパネル行を挿し、別の行を開くと前のパネルは閉じる。取りやめで焦点を押したボタンへ戻す。
  - 既存の完全一致の契約（列見出し・セルの行列・ボタン一覧・セル数・種類別チェック・面をまたぐ幅の検査の対象）を、この変更に合わせて同じタスクで更新する。「危険の通知がちょうど 1 件」の既存の経路表には「編集の拒否」の経路を 1 つ足すだけにする（通知の整理そのものは 3.4 が作る）。
  - 完了状態: 画面のテストで、パネル行が対象行の直後にあり列数と一致する・同時に開けるのは 1 つ・取りやめで焦点が戻る・全行に編集ボタン・表示名の列・自分の行で開いたパネルはロールと所属が固定表示になる、が緑で、更新した既存契約もすべて緑。
  - _Requirements: 1.1, 2.2, 6.1, 6.2, 6.3, 6.4, 6.5, 6.10_

- [ ] 3.4 保存の成功・失敗の通知と、代理店一覧の取得状態
  - 保存に成功したら、一覧を取り直し、焦点を編集ボタンへ戻し、パネルを閉じ、成功の通知を読み上げ領域で出す。
  - 編集を始めたときにページの操作エラーと成功通知を消し、パネル内の失敗と合わせて危険の通知を 1 件に保つ。成功通知は次の操作（編集の開始・登録・無効化・有効化）で消す。
  - 代理店一覧の取得失敗を記録してパネルへ伝える（登録フォームの既存の挙動は変えない）。
  - 完了状態: 画面のテストで、次がすべて緑。dashboard-web の型検査・lint・ビルドも緑。
    - 成功時の取り直し・焦点・閉じる・成功通知
    - ページに操作エラーまたは成功通知が出ている状態から編集を始め、保存に失敗しても一覧は取り直されず、危険の通知は 1 件で、成功通知も消えている
    - 代理店一覧の取得に失敗した状態でパネルを開くと、ロールと所属が固定表示になる
  - _Requirements: 1.12, 1.14, 6.5, 6.6_

- [ ] 4. 統合検証と受け入れ
- [ ] 4.1 E2E の検証面に編集パネルの状態を登録
  - 一覧を開いてから、代理店・無効化済みの利用者の編集ボタンを完全一致の名前で押す手順を fixtures に足す（画面遷移は増やさない）。前提の assert はパネルにしか無い要素にする。
  - 検証面の一覧へ登録し（a11y 監査は一覧から自動で生成される）、携帯端末の幅の横スクロール実測を 1 本足す（捲れる領域は帯と表の 2 件のまま）。この実測はページ全体のはみ出しだけを見る。パネルは表の捲れる容器の内側にあるので、パネルの中身が溢れても赤にならない。その網は 4.2 の実ブラウザ観察である。
  - 「7 面」の表記を 8 面へ直す（CI のワークフローのステップ名を含む。ワークフローを含む push には workflow の権限が要る）。
  - 完了状態: 自分で立てたサーバに向けたローカルの Playwright で、a11y 監査と各面の実測が新しい面を含めて緑。パネルへ既知の違反を 1 つ入れるとその面だけが赤になることを確かめて戻し、その対照を記録している。画面遷移の所有権と監査の前提のガードが緑。
  - _Requirements: 6.8, 6.9_

- [ ] 4.2 全体受け入れ（main との再照合・全ゲート・非回帰・実ブラウザ）
  - 提出の直前に main を取り込む。migration の番号ガード（HEAD と main を合わせて見る）と、0009 が外す制約の名前を取り直す（#252 などが先に番号や監査の制約に触れていないか）。
  - TS 全体の lint・型検査・ビルド・テスト（隔離 DB の DB テストを含む）、DB の網羅スイートの実行装置、Go のテスト（0009 は Go の隔離 DB と言語をまたぐ契約テストにも適用される）、ts-ci が呼ぶ CI ガードをすべて流す。
  - 登録・無効化・有効化の既存挙動が変わっていないことを、既存テストの緑で確かめる。
  - 実ブラウザで携帯端末の幅のパネル（代理店の利用者・自分の行・一覧取得失敗）を開き、表の中ではみ出して操作できない箇所が無いことを見る。
  - 完了状態: 上記がすべて緑で、実ブラウザでの観察（何を見たか・見ていない軸）を記録している。
  - _Requirements: 6.9_

- [ ] 4.3 本番反映（運用者の明示の承認を得て実施）
  - 事前確認: `db/migrations/` の全番号の対象が本番にあること、旧制約の実名、表の所有者。proxy は 5432 以外のポートで張り、接続先が本番であることを確かめてから流す。
  - 0009 を**マージより前に**適用し、制約の定義に新しい 4 値が入ったことを確かめる。
  - マージ → デプロイの結果を自分で確かめる。本番で自分の表示名を変えて元に戻し、監査記録に表示名変更の行が 2 行増えることを確かめる（検証用の利用者・店舗は新しく作らない）。
  - 完了状態: 本番の制約が 16 値になり、実操作の監査行が観測され、実施記録（識別子は先頭 8 文字まで）を本ファイル末尾に残している。
  - _Requirements: 5.1, 5.2_

## Implementation Notes

- 1.1 旧制約の実名は隔離 DB（PG 16.14）で `audit_logs_action_check` と再実測した（audit_logs の CHECK はこの 1 本のみ）。赤化の対照（0009 の変異→実行→元へ戻して一致を確認）:
  - 値を 1 つ落とす（`dashboard_user_agency_updated`）→ 集合一致テストと SQL 8.1b が赤。
  - 既存値を落とす（`owner_created`）→ 集合一致テストだけが赤。
  - DROP を消して新旧を共存させる → SQL 8.1a だけが赤。
  - DROP の名前を違える → migration の適用自体が失敗する。
  - `as const` を外す → 型検査が赤（TS2578）。
  - 既存 12 値の欠落は TS 側、旧制約の残存は SQL 側が捕まえる（役割分担）。独立レビューで同種の対照を再現済み。
- 1.1 `scripts/check-db-ordinals.sh` は追跡下（`git ls-files --cached`）しか見ない。新規 migration は `git add` の後に流さないと検査されない。
- 1.2 「差分が無ければ更新しない」は、指定列だけを SET し、WHERE に `IS DISTINCT FROM` の述語を付けた UPDATE の 0 行更新で実現した。
  - 比較は DB の型（uuid）で行う。
  - 0 行のときは、取得済みの行を現状として返す。
  - 行を書き換えないことは、テストで `xmin` の一致を assert して固定した（値の一致だけでは「同じ値で UPDATE する」実装が素通りする）。
  - 評価順・残数・差分の述語・戻り値の変異は、実装担当と独立レビューがそれぞれ当てて赤を確認した。ロックの除去は逐次テストでは検出されないので、1.3 が対照を取る。
- 1.3 並行テストは f9c の帯を使い、組み合わせごとにテナントを分けた。別の接続でロックを保持 → 本物の関数が 500ms 未解決 → 確定 → 最新状態を観測、の形。赤化の対照は実装担当と独立レビューの双方で再現し、毎回 src を戻して `cmp` で一致を確かめた:

  | 変異 | 赤になるテスト | 緑のまま |
  |---|---|---|
  | 更新の関数からロックを外す | (a)・(c)（500ms 未解決の assert） | (b)・f8c |
  | 待機の assert も外す（探査） | (a) が `updated` を返し、確定後に有効な運営が 0 人になる（write-skew の実在を観測） | — |
  | ロックを残数判定の後へ遅らせる | (a)・(c)（結果の assert） | — |
  | 無効化の関数からロックを外す | f8c・(b) | — |
  | 共有の定数値を変える | 並行 4 本すべて | — |

  - 相互排他は 2 組で押さえている。更新側が (a)(c)、無効化側が (b) と f8c。
  - 評価順「所属先 → 残数」は逐次テスト 1 本で固定した。入れ替えの変異で、そのテストだけが赤になる。
- 1.3 `ts/packages/db/test/pool-connector.test.ts:126`（`activeTimeoutCount()` の基準値）が、変異の実行中に 1 回だけ落ちた。
  - 原因は既存の競合と判断した。`process.getActiveResourcesInfo()` は vitest 自身の ref 付きタイマーも数えるので、負荷で競合の窓が広がると数が 1 ずれる。
  - f9c とは無関係である。テストファイルは forks で別の子プロセスに分かれて走る。
  - 再現は 0 件（単体 20 回・負荷下 20 回・全体 8 回）。直すなら別 Issue で扱う。
- 2.1 正規化の対照: `handleDashboardUserUpdate` の `req.id.toLowerCase()` を外すと、大文字 ID のケースだけが 5 件赤になった。
  - 内訳は、自己拒否 3 件、自分の表示名だけのときに依存へ渡す引数 1 件、依存へ渡す ID の正規化 1 件。
  - 小文字 ID の同じ形のケースは緑のまま。独立レビューで再現済み。
- 2.1 入力検証のテストは、誤った項目に正しい別の変更（表示名）を添えた body で書く。
  - 誤った項目だけの body だと、「変更が無い → 400」の包括規則が同じ 400 を返す。そのため、未知の role や、role 無しの不正な agencyId を黙って捨てる変異が緑のまま通った（独立レビューの 1 回目で差し戻し）。
  - 組み合わせたケースを足した後は、どちらの変異も赤になった。
  - 認証・認可を UUID の検証より先に行う順序も、未認証・代理店に不正な id を送るテストで固定した（順序を入れ替える変異で赤）。
- 2.1 依存へ渡す部分更新の入力は、`mock.calls` を `toStrictEqual` で比べる。`toHaveBeenCalledWith` は、キーが無い状態と `undefined` のキーを区別しない。
- 2.2 ルート統合の対照（すべて原本へ戻して `cmp` で一致を確認）:

  | 変異 | 結果 |
  |---|---|
  | ルート名を壊す | 10 件赤。予約パスのガードも新ルートを走査している（ルート数 19→20） |
  | テストの `buildApp` から監査の実物を外す | 成功系 3 件だけが赤（`expected +0 to be 1`）。拒否側の「増えない」は、発火を実測済みのカウンタで書いている |
  | 拒否でも監査を書く | 該当テストが赤 |

  - 未配線のルートは Hono が平文の 404 を返す。そのため 404 を期待するテストは、`error.code` まで assert しないと赤にならない。
- 2.2 合成根 `src/index.ts` の配線は、どの検査にも守られていない（既存の全ルートも同じ状態）。
  - `id` と `operatorId` を入れ替えても、`userUpdate.auditLog` を消しても、型検査と全テストは緑のままだった。
  - テストの `buildApp` は合成根の複製なので、捕まえられるのは複製側の取り違えだけである。
  - 既存コメント `test/app-routes.db.test.ts` の冒頭（「配線の整合を機械検証する」）と `email_conflict_disabled` の assert の直前（「index.ts … 転置すると … 決定的に捕捉する」）は、index.ts については事実でない。
  - 構造の変更が要るので、この機能の範囲外として別 Issue で扱う。
- 3.1 `updateDashboardUser` の形は design の正典どおり `({ id, changes }, options)` にした。パネルが受け取る `updateUser?: (id, changes)` とは形が違うので、3.2 の既定値はアダプタで包む。
  - 空の変更 `{}` は型では許されている。送ればサーバが 400 を返すので、「変更が無ければ送らない」はパネル（3.2）が固定する。
  - クライアントの body を本物の `handleDashboardUserUpdate` へ流す突き合わせを独立レビューで行い、10 形すべてがサーバで意図どおりに解釈されることを確かめた。
  - `JSON.parse` した body には、値が undefined のキーが残らない。そのため `toEqual` と `toStrictEqual` で検出数に差は無い。「表示名を常に代入する（undefined のまま）」は送信内容が変わらない等価な変異である。
- 3.2 design の細部を次のように補った。どれもテストで固定し、独立レビューで spec と矛盾しないと判定済み。
  - (a) 現在の所属が選択肢に無い場合（一覧の読み込み後に作られた代理店など）は、id を名前の代わりにした選択肢を 1 つ足す。編集できる状態でも、現在の所属を未選択に見せないため（1.1, 1.14）。
  - (b) 表示名は、触れていなければ送らない。入力値が開いた時点の `displayName ?? ''` と同じなら、trim 後の比較をしない。登録は trim しないので、前後に空白を持つ保存値があると、開いただけで「空白を取る変更」が送られてしまうため（3.1）。design の字義と違うのは、保存値に前後の空白があるときか `''` のときだけである。
  - (c) 比べる相手は、開いた時点の `user` を `useState` で固定したもの。props に追従させると、他の運営が昇格させた対象への所属の移動が「代理店にする」に化ける。
  - (d) 所属未選択の案内は、送信の拒否と同じ単一の destructive Alert の枠で出す（常に 1 件・6.6）。
- 3.2 Base UI 1.6 の振る舞い 2 点:
  - `focusableWhenDisabled` の Button は、押せるときも `aria-disabled="false"` を描く。「押せる」の assert は属性の不在で書かず、「値が true でない」と「`data-disabled` が無い」で書く。
  - native の button には、既定で `type="button"` が付く。そのため `type` の欠落は描画の検査では見つからない（等価な変異）。
- 3.2 → 3.3 への申し送り:
  - 4 面の幅の検査は `test/admin-users-page.test.tsx` の `SOURCE_ROOT = src/app` を起点にしている。`src/components/` のパネルを対象へ足すには、パスの解決を広げる。
  - パネルは開いた時点の値に固定するので、行ごとに作り直す（`key` を利用者 ID にする）。
  - パネル行の `aria-controls` 先の ID は、パネル内の `user-edit-{role,role-hint,agency,display-name,locked-reason,error}-${id}` と重ならない接頭辞にする。
- 3.2 → 3.4 への申し送り:
  - 送信中もキャンセルは押せる。キャンセルした後に保存が成功すると、パネルは `onSaved` を呼ぶ。取り直しと成功通知は常に行うが、閉じる・焦点を戻すのは「保存した利用者のパネルがまだ開いているとき」に限る（別の行のパネルを閉じたり、焦点を奪ったりしないため）。
  - 保存の開始時に前の警告を消す処理（`setError(null)`）が、どのテストにも守られていない。「失敗 → 再度保存（送信中）で alert が 0 件」を 1 本足す。
- 2.1/2.2 src のコメントで Issue を指すときは `Issue #NNN` と書く。種別の無い `#250` は `scripts/check-design-tokens.sh` に 3 桁の hex 色として検出され、ts-ci が赤になる（2.1 のコミットで入り、2.2 のレビューが全ガードを流して見つけた）。タスクごとの検証には、ts-ci が呼ぶ `scripts/check-*.sh` を全部含める。
