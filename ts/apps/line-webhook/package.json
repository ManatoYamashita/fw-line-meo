{
  "name": "@fwlm/line-webhook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.scripts.json",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "eslint src test scripts",
    "test": "vitest run --passWithNoTests",
    "setup-rich-menus": "node dist-scripts/scripts/setup-rich-menus.js",
    "relink-completed-menu": "node dist-scripts/scripts/relink-completed-menu.js"
  },
  "dependencies": {
    "@fwlm/db": "workspace:*",
    "@fwlm/design-tokens": "workspace:*",
    "@fwlm/line-report": "workspace:*",
    "@fwlm/observability": "workspace:*",
    "@fwlm/store-identification": "workspace:*",
    "@hono/node-server": "^1",
    "@line/bot-sdk": "^11",
    "hono": "^4"
  }
}
