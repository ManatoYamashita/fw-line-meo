{
  "name": "@fwlm/delivery-job",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "eslint src test",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fwlm/db": "workspace:*",
    "@fwlm/design-tokens": "workspace:*"
  }
}
