# ルート出力（gcp-infra-foundation）
#
# 実 output（service URL・SQL 接続名・WIF provider 名 等）は Task 4.3 の
# root 配線時に、各モジュール呼び出しの output を参照して定義する。
# 本骨格段階（Task 1.1）では意図的に空とし、モジュール未配線の状態でも
# `terraform validate` が成立するようにする。
